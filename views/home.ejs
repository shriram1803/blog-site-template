<%- include('partials/header.ejs'); %> 
    <h1>Home</h1>
    <p><%= Paragraph1 %> </p>
    <% postArray.forEach(function(element){%>
        <h1><%= element.title %></h1>
        <p><%= element.content.substring(0, 120) + "..." %> <a href="posts/<%= element.title %>">Read More</a></p>
    <% }); %>  
<%- include('partials/footer.ejs'); %> 